# 2026-09-24

## Mock animation — spec drafted

- Founder asked for scroll-triggered animation on the mocked screens, to show
  how each app works. Pilot: Accounting.
- Worktree `.worktrees/mock-animation`, branch `mock-animation`.
- Spec: `projects/mock-animation/specs/mock-animation.md`. It is CSS only and
  reuses the existing `.reveal` → `.is-visible` observer. The final frame must be
  today's markup, and every step traces to a line of `products.ts`.
- Found: the reduced-motion block in `global.css` zeroes the duration but not
  the delay. The spec adds `animation: none` for the new steps.
- Verified: nothing yet. No code has been written.
- Left: founder approval (two open questions: cards too? pace?), then the plan
  and tasks.json.

## Mock animation — Accounting pilot built

- Founder approved the spec, with cards included. Plan: `projects/mock-animation/plans/pilot.md`.
- Changed: `global.css` (a `.mock-step` rule gated on `.reveal.is-visible`, plus a
  reduced-motion `animation: none`), the three Acct mocks (attributes only), and
  `scripts/verify.mjs` section 9.
- Verified:
  - `npm run build` passes (18 pages). Section 9 failed 3/3 before the code and
    passes 3/3 after.
  - Mutation tests: removing the `.is-visible` gate, or the reduced-motion
    override, from the built CSS makes the matching check fail.
  - Built HTML with the step attributes stripped is identical to a `main` build,
    on `/products/accounting/`, `/` and `/products/`. The steps total 23 on the
    product page (7 + 9 + 7) and 7 on each card page.
  - In the browser, hero step opacities at 950ms were
    `1.00 0.99 0.79 0.00 0.00 0.00 0.00`, and all 1.00 by about 4s.
  - With the reduced-motion block forced on (browse's CDP allowlist blocks media
    emulation, so the built CSS was rewritten temporarily and then restored),
    every step is at 1.00 with `animation-name: none` at 120ms.
  - Screenshots, mid-sequence and settled, in light and dark plus the card, are
    at `/private/tmp/mockanim/`.
- `npm run check`: 0 errors, 0 warnings, 0 hints.
- Not run: the Lighthouse CLS check from the spec. Only opacity and transform
  animate, which do not count toward CLS, but that was not measured.
- Left: founder review of the pace, then merge. Rollout to the other 33 screens is
  a separate plan.

## Mock animation — revision 2 built (Accounting)

- The founder asked for problem → "our solution" (shorter) → steps with screens,
  and for the UI to be researched by a subagent rather than guessed. Research
  (three passes) found that the live Accounting copy and screens described an
  unshipped engine. The founder chose "the shipped app only", approved the new
  copy, and switched steps 3 and 4.
- Built:
  - `solution` and `steps[].screen` in `products.ts`.
  - Five new screens; the three old ones deleted.
  - A `.mock-before` / `--swap` primitive.
  - A template branch for steps with screens, with the gallery hidden.
  - The depth check replaced by the solution-length rule for Accounting.
  - A new section 10: no retired claims, and six screens.
- Verified:
  - `npm run build` passes (18 pages); `npm run check` shows 0 errors.
  - Creators Sphere, KYC and Shopmgr built HTML is identical to `main`.
  - In the browser, the step 2 swap shows Processing at 0.9s and Pending at 2.8s.
  - With reduced motion forced on, the final state shows at 0.15s with nothing
    hidden.
  - Screenshots are in `/private/tmp/mockanim2/`.
- Left: the independent label review (Sonnet, running), then the founder's review.
- The independent label review (Sonnet) came back. Fixed:
  - the bank suggestion now arrives with its line, not after (the server
    pre-selects it on load);
  - "Task #N" titles became the app's real page title "Task Details"
    (`task_detail.page_title`);
  - the unverified metas "Pending" and "Bank statement" were dropped;
  - the matched bank amount is now −$79.04, which agrees with 104.00 SGD × 0.76.
- Not applied, flagged to the founder:
  - the reviewer wants steps 3 and 4 swapped back, because in the code the
    question comes before the draft. The founder asked for this order.
  - The reviewer says step 4's title "Only the exceptions reach you" is too
    strong, since approval reaches the person every time. The founder approved
    that wording; the body does say "a single tap approves".
- Rejected: the reviewer's step 5 "everything → confirmed entries". The close
  guard blocks ending a period while drafts exist, so everything up to that date
  is read-only afterwards.
- Build: all checks pass (18 pages). Bank sequence is now 0–3.
- Founder: step 4 title → "Human in the loop" (replaces "Only the exceptions reach you"). Build passes.
- Founder: "merge it". Taken as sign-off on the pace, on steps 3/4 as switched, and on step 1 as built.
  Task 9 closed; branch `mock-animation` merged to main; worktree deleted.
- Still open: do beta users have the phone app? Step 1 names it. If not, switch it to the web "Upload Bills" modal.

## Mock rollout — planning

- Pushed main to origin (9709c0f..a307cc0), including the doc-updater commits 8176270 and a307cc0.
- Founder: "push it, then plan for all other products / works."
- Spec + plan drafted on branch `mock-rollout`: 3 visible products + 8 work pages; Leave and 1line.ai stay hidden.
- Research not started: held until the founder answers the spec's 4 open questions, so no spend on 8 work repos if the answer is "products only".
- The 2026-09-17 repo mapping is gone with its scratchpad; research will re-derive it and check it against the capability-depth repo counts.
- Research back for all three (Sonnet). Two agents reported "paused per user request"; I sent no pause. Notes were complete except Creators Sphere's missing file:line citations (flagged in the note) and one wrong claim, fixed by hand: proof checks are queued background jobs (`@is_background_job`), not inline.
- KYC: the shipped generation has no reviewer, no one-time code, no expiry, no event log; it auto-marks a profile verified after one saved document. Live copy claims all four.
- Shopmgr: holds up; false = staff review/rate conversations, WooCommerce in practice. Creators Sphere: false = balance/payout, slot release, hashtag check, creator insights.
- Copy proposals: `projects/mock-rollout/specs/copy-batch-a.md`.
- Founder approved batch A copy; KYC describes what shipped; currency S$; work pages get the full pattern. KYC step bodies written by me from the research (founder approved titles only).
- Repo is PUBLIC: some notes on this branch were rewritten before push; branch to be squash-merged; work research kept in scratchpad. Lesson added. KYC research also says the current generation 'was never shipped as a product' — to confirm with the founder. Batch A: 3 Sonnet builders (5 mocks each). Batch B: 8 Sonnet researchers.
- Task 8 done (6a7fe10): work.ts gains optional solution + steps; work page renders them like products and drops long form, triad and gallery; verify's solution rule shared by products and work. Proven: unchanged pages byte-identical; a test entry built 3 screens with no long form; a 3-sentence solution FAILs.
- KYC + Shopmgr built and wired (119a9c7): 10 mocks by Sonnet builders, wired by me. verify 9 gains kyc/shopmgr step maxima (they are maxima, not counts, so the first try failed on off-by-one — corrected); verify 10 generalised to a per-product retired-claims table (shopmgr: rate answers / review conversations / WooCommerce; kyc: reviewer / one-time code / expir / event log / verified / stamped). ShopTicketMock had two `--step: 5`; the date change is now step 6. 5 old mocks deleted; ChatMock kept for the home hero. Build: all checks pass, 18 pages; astro check 0 errors.
- The Creators Sphere mock files were swept into 119a9c7 while their builder is still running; not wired, so inert. Re-commit when it reports.
- Flag for cleanup (not this branch): AcctUploadMock and AcctQuestionMock use `text-bg` on `bg-action`; the rest of the mocks use `text-on-action`.
- Work research so far: bundling (library only, no UI), awards (voting per candidate page, no category close), incident (repo was not the candidate; audio/report stubs), tender (staff-only tool — no bidder surface, no ranking, no award).
- Creators Sphere built and wired (aa377c1). Fixed on wiring: builder used `text-bg` (switched to `text-on-action`, the majority token); Submit screen drew the manual URL field under a picked thumbnail, which the app never does (removed); approved solution was 3 sentences, over the rule (joined to 2, same content); retired-claim pattern `/get paid/` hit the app's real "YOU GET PAID" pay label, so it now targets the old tagline only (proven to still match it). Old Creator mocks deleted. All checks pass, 18 pages; astro check 0 errors. Non-product pages differ from before only by the CSS file hash; home and products index differ by the new cards; home hero still ChatMock.
- All 8 work researches in. Repo mapping re-derived, no overlaps; three of my candidate guesses were wrong (incident, CMS, SharePoint) and the agents found the right repos.
- Batch B copy: `specs/copy-batch-b.md` (site-safe only; scanned for known client terms: 0 hits). Changes from the agents' drafts: awards drops "review & shortlist" (done by hand, nothing to draw); SharePoint "built into" → "around" the tenant; bundling drops the script-conversion line.
- Label review of the 15 batch A mocks running (Sonnet).
- Label review (Sonnet) fixed in c1d4a24: Shopmgr typing state is dots (the quoted line was a double-send toast); KYC portal rows show the holder, not the counterparty; async results (proof verified, indexing success) get --mock-lead-in 1600ms so they land ~1.36s after the prior beat; KYC citation line numbers corrected; invented 'Scan with the phone app' caption dropped; location hints removed from code comments. Browser pass (preview :4333): all 6 frames per page reveal on real scroll (script scrollIntoView does not fire the observer headless — Accounting behaves the same, so it is the harness); settled frames complete; no horizontal overflow. Founder approved batch B copy: drop 'marketplace', drop 'works', no awards comments step.
- Batch B: 8 Sonnet builders (brief in the session scratchpad, public-repo anonymisation first). Work index corrected (cd0adc3): 6 of 8 approach/outcome/angle lines repeated refuted claims.
- New verify check (370805d): a longer --mock-lead-in on a middle step made later steps arrive first (found by the tender builder). Every screen on every page must now start its steps in order and time each swap with its step. Proven to FAIL on a planted mid-step lead-in; restored.
- 7 work pages wired (af1204c). verify tables are now page-keyed (products/…, work/…) with per-page step maxima and retired claims. On first build the checks caught: 3 screens out of order (SharePoint link check, checked-out report, scheduling settlement — later steps now share the lead-in), a repeated step (SharePoint audit), and 3 over-broad retired patterns ("award" hit the Awards link, "declaration" a real checklist item, "works" the "How it works" heading — narrowed to the refuted wording). All pass; astro check 0 errors.
- Awards builder still running.
- Awards wired; all 8 work pages done (904f937). 22 old work mocks retired (none imported elsewhere). `npm run build` passed while `astro check` had 1 error (a union member missed) — build does not type-check, so run both. Fixed; both clean.
- Client-term scan of all 31 work mocks: 0 hits. Two Sonnet label reviews running (4 pages each), anonymisation first.
- Work label reviews (2× Sonnet) fixed in 6d37574 and 8b611ce: scheduling confirm control was a provider-only detail control drawn in the customer list (now the provider's view); others' slots show "Not available"; CMS preview address is fixed, not a hash; neutral example name; SharePoint example names no longer echo another page's distinctive nouns; tender extract names genericised; SharePoint flagged row uses "Edit" (the level the code flags); contract confidence is numeric. Anonymisation clean in both reviews.
- Browser pass, all 8 work pages: every screen reveals on real scroll, settled frames complete.
- Phone width (390px) found real clipping: a screen with unwrappable content widened the one-column grid, so step text was cut off — including on the LIVE Accounting page (heading at 398px). Fixed with grid-cols-1 on the page grids and w-full on the auto-margined screen wrappers (0db0468, 293ef9d). Now 0 frames and 0 text past the edge on all 11 pages plus home, /products, /work. Desktop widths unchanged.
- Before handoff: notes scrubbed of client pairing, security specifics and location hints (d8c2b8b). Work and product raw research copied outside every repo to ~/Workspaces/0atlas/successiv/research-private/2026-09-24/ (owner-only), since the job scratchpad is temporary. Awaiting founder review; then squash-merge with a fresh message, build + check on main, remove worktree, push, doc-updater.
- Founder: "publish KYC as is, merge and push it". Squash-merged to main; Accounting phone-app question stays open.
