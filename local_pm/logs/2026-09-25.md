# 2026-09-25

- Founder: Accounting beta users have the phone app. Step 1 ("from the web or the phone app") and its phone-chrome screen stand. Open question closed on the board.
- Phone mocks now keep real phone proportions. Founder asked for Creators Sphere screens to look like a real phone, or drop the tab bar if half height. The `phone` shell in `MockFrame.astro` gets `min-h-[560px]` and a flex column, so every phone frame is 272×572 (≈1:2.1) with empty screen above the tab bar; before, frames were 241–524 tall. Shared shell, so KYC's four and Accounting's one phone screen changed too. Verified: `npm run check` 0 errors, `npm run build` all checks passed (18 pages), screenshots light/dark/375px/product card.
- Checked and cleared: at 375px `/products/creators-sphere/` reports `scrollWidth` 391 (the `mock-wash … -inset-10` glow), but the page does not scroll (`scrollX` stays 0 after `scrollTo(100, …)`) and no heading, paragraph or `.mock-frame` ends past the edge. Not a bug; an earlier note here called it one.
- Reversed the tall phone the same day. Founder: 572px frames were too tall for short copy; if half a screen, cut it off nicely. The `phone` shell now draws only the top of the device: rounded top, no bottom edge, no tab bar, and a 40px mask fade at the bottom over 36px of spare screen so the last row stays whole. Frames are back to content height (Creators Sphere 241–524). Shared shell, so KYC and Accounting follow. Verified: check 0 errors, build all checks passed, screenshots desktop light/dark, KYC at 375px.
- Shipped: b02b905 (tall phone) then b8167b8 (cut-off phone), both deployed to Pages and confirmed live. Founder signed off on the cut-off version.
