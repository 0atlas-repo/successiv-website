# 2026-09-26

- Home hero replaced with a wall of mocked screens (founder request, Pryzm reference). Three treatments previewed under `/lab/` on branch `mock-wall` (e5fed57): drift, drift + pointer parallax, drift + looping mock steps. Founder picked drift + mock steps.
- Shipped on the branch: `src/components/MockWall.astro` is the home hero. 25 screens from the 12 published entries (4 products, 8 work), round-robin so neighbours differ, no screen repeated. Each card captioned with entry, kind and step title from `products.ts`/`work.ts`, and linked to its page. Badge counts both numbers from data. Columns drift; each screen plays its steps on reveal, then one on-screen card replays every 1.8s (paused when the tab is hidden or the hero is scrolled past). Reduced motion stops drift and replay.
- Removed: `/lab/` preview pages, `Hero.astro`, and `mocks/ChatMock.astro` (only the old hero used it). The old hero's kicker line and faint logo ribbon went with it.
- Doc fix found on the way: no product has set `cardScreen` since 02def72, but CLAUDE.md and the `products.ts` comment still said Shopmgr did. Both corrected; behaviour unchanged.
- Second hero button is now "Browse the work" (was "Talk to us", which the header already carries).
- Verified: `npm run check` 0 errors/warnings; `npm run build` all checks passed (18 pages, 31 labelled mockups on home); screenshots desktop light/dark and 390px; mobile `scrollWidth` 390; step animations confirmed replaying in the browser. Not checked in a browser: the reduced-motion path (CSS/JS guard only).
- Shipped: founder said merge and push. main fast-forwarded to 2216c9e, Pages deploy run 36163293674 succeeded. Live home shows 25 wall cards and the badge, no console errors; `/lab/wall-steps/` returns 404. Worktree and branch removed.
- Reduced-motion path checked on the live site with Playwright (`reducedMotion: 'reduce'` vs `'no-preference'`, 1280x800). Reduce: all 5 drift columns report `animation-name: none` and move 0px over 5s; 0 wall style writes in the 6s after load (25 at load are one-time card setup), so the replay loop never starts; 0 on-screen steps below full opacity; screenshot shows settled screens. No-preference control: columns move 15-20px, 40 replay writes in 6s. gstack browse could not do this (`Emulation.setEmulatedMedia` is not in its CDP allowlist), so the script used gstack's bundled Playwright.
- `docs/BRIEF.md`: "Creator Sphere" corrected to "Creators Sphere" (2 lines). CLAUDE.md note updated to match. `COPY.md`, `hiring/DESIGNER_BRIEF.md` and `references/DESIGN_REFERENCES.md` still use the singular; left alone, out of the task's scope.
